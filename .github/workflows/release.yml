name: Release

# Push a git tag (e.g. `git tag v0.1.0 && git push --tags`) to trigger a
# packaged release upload to CurseForge (and optionally WoWInterface/Wago).
on:
  push:
    tags:
      - '*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Package and release
        uses: BigWigsMods/packager@v2
        env:
          CF_API_KEY: ${{ secrets.CF_API_KEY }}
          # Optional: also publish to WoWInterface and/or Wago if you set
          # these secrets up later. Safe to leave unset/unused.
          WOWI_API_TOKEN: ${{ secrets.WOWI_API_TOKEN }}
          WAGO_API_TOKEN: ${{ secrets.WAGO_API_TOKEN }}
